
<div class = "container-fluid" >
  <div class = "row">
     <div class = "col-md-12 col-xs-12 ">       
           <div class = "col-md-4 col-md-offset-4">
            <p class ="">보내고 싶은 메시지를 작성하세요.<br>
             <% flash.each do |type, message| %>
<div class="alert <%= bootstrap_class_for(type) %> alert-dismissable fade in">
<button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
<%= message %>
</div>
<% end %>
             <%= form_for @new_post, url: send_sms_path(@id), method: "get" do |f| %> 
                <div class="form-group">
                  <%=  f.text_area :context, class: 'form-control', :size => "24x6" %>
                 </div>
                 <div class="form-group">
                  <%= recaptcha_tags :ajax => "true" %> 
                </div>                
                <div class="form-group">
                  <%=  f.button :submit, class: 'btn  btn-lg btn-primary' %>
                </div>    
            <%end%>
                        
              <br>
              <span style = "font-size :20px;color:rgba(255,255,255,0.8)">상대방이 동의하면 핸드폰으로 상대방의 번호가 전달됩니다.</span>
            </div>
         </div>
        </div>
    </div>
